import { Roboto } from 'next/font/google';

export const robotoRegular = Roboto({
  weight: '400',
  subsets: ['latin'],
});

export const robotoBold = Roboto({
  weight: '700',
  subsets: ['latin'],
});
